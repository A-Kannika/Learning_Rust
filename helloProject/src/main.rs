// using cargo to create a new project
// cargo new (name of a project)
// use: cargo run to run it --> don't need to recompile it again whn you change the code

fn main() {
    println!("Hello, Rust from Cargo!");
}
